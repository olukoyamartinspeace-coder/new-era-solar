declare module "next/dist/lib/metadata/types/metadata-interface" {
  import type { Metadata, ResolvingMetadata, ResolvingViewport } from "next";
  export type { Metadata, ResolvingMetadata, ResolvingViewport };
}

declare module "next/types" {
  import type { Metadata, ResolvingMetadata, ResolvingViewport } from "next";
  export type { Metadata, ResolvingMetadata, ResolvingViewport };
}
